version: 2

models:
  - name: mart_customer_summary
    description: "Customer-level transaction summary"
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
      - name: total_amount
        tests:
          - not_null
